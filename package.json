{
  "name": "ghl-deal-ledger",
  "version": "2.0.0",
  "private": true,
  "description": "Cloudflare Worker that syncs GHL signed-document emails to a SharePoint Deal Ledger by parsing attached contract PDFs",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test-parse": "node src/test-parse.js"
  },
  "dependencies": {
    "pdfjs-dist": "^4.4.168"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
